package training.scalaTraining

import org.apache.avro.ipc.specific.Person

/*


class DVSJulyEncapsulation(val name:String, age: Int) {
    val location = "Bangalore"
    protected val salary =  10000
    private val pin: Int = 1234
    def personDetails() = println(s"Person name is $name and age is $age and salary is $salary")
    private def salaryInfo() = println(s"Person salary is $salary")

    def checkAccess() = {
      println("check if access is allowed")
      salaryInfo()
    }

  class Child(name:String, age: Int) extends Person(name, age) {
    def parentSalary = println(s"parent salary is $salary")
      location
  }

  object EncapsulationExamples extends App {

    val p = new Person("Prasad", 30)
    println(p.location, p.name)
    p.personDetails()

    val c = new Child("Scala", 15)
    //c.salary
  }
  }


*/
